name: Continuous Deployment

on:
  # Allow triggering manually. 
  workflow_dispatch:

permissions:
    id-token: write
    contents: read
jobs:
  ci:
    uses: ./.github/workflows/ci.yml

  deploy_infra:
    needs: [ci]
    uses: ./.github/workflows/provision.yml
    secrets: inherit

    # TODO: Fix this.  The steps below depend on the Azure resources provisioned in provision.yml.
    #       Currently use GitHub repo variables.

  push_to_acr:
    needs: [deploy_infra]
    uses: ./.github/workflows/build_and_push.yml
    secrets: inherit

  deploy_to_container_app:
    needs: [push_to_acr]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@main

      - name: Azure authentication
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: "Deploy ServiceBusWorker to Container App"
        uses: azure/arm-deploy@v2
        with:
          deploymentName: 'ServiceBusWorkerDeployment-${{ github.run_id }}'
          scope: resourceGroup
          resourceGroupName: ${{ env.INFRA_resourceGroupName}}
          template: ./infra/worker-app.bicep
          parameters: blobConnectionString=https://${{ env.INFRA_storageAccountName}}.blob.core.windows.net 
                      imageName=${{ env.INFRA_acrName}}.azurecr.io/service-bus-worker:${{ github.sha }} 
                      managedEnvironmentName=${{ env.INFRA_managedEnvironmentName }} 
                      openAIConnectionString=https://${{ vars.INFRA_AZURE_OPENAI_MODEL_DEPLOYMENT_NAME}}.openai.azure.com/ 
                      openAIModelDeploymentName=${{ vars.INFRA_AZURE_OPENAI_MODEL_DEPLOYMENT_NAME }} 
                      registryName=${{ env.INFRA_acrName }} 
                      serviceBusConnectionString=${{ env.INFRA_serviceBusEndpoint}}.servicebus.windows.net 
                      serviceBusQueueName=${{ vars.INFRA_SERVICE_BUS_QUEUE_NAME }} 
                      userAssignedIdentityName=${{ env.INFRA_userAssignedIdentityName }}

  #     # TODO: Set up a semantic version strategy for the container image
  #     # TODO: Consider switching to legacy ARM deployment tasks
  #     #       - https://github.com/Azure/bicep-deploy/issues/88
  #     #       - https://github.com/Azure/bicep-deploy/issues/96
  #     - name: Deploy ServiceBusWorker to Container App
  #       uses: azure/bicep-deploy@v1
  #       with:
  #         type: deployment
  #         operation: create
  #         name: 'ServiceBusWorkerDeployment-${{ github.run_id }}'
  #         scope: resourceGroup
  #         subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  #         resource-group-name: ${{ vars.RESOURCE_GROUP_NAME }}
  #         template-file: ./infra/worker-app.bicep
  #         parameters: '{"blobConnectionString": "https://${{ vars.INFRA_STORAGE_ACCOUNT_NAME}}.blob.core.windows.net",
  #          "imageName": "${{ vars.INFRA_ACR_NAME }}.azurecr.io/service-bus-worker:${{ github.sha }}",
  #          "managedEnvironmentName": "${{ vars.INFRA_MANAGED_ENVIRONMENT_NAME }}",
  #          "openAIConnectionString": "https://${{vars.INFRA_AZURE_OPENAI_NAME}}.openai.azure.com/",
  #          "openAIModelDeploymentName": "${{vars.INFRA_AZURE_OPENAI_MODEL_DEPLOYMENT_NAME}}",
  #          "registryName": "${{vars.INFRA_ACR_NAME}}",
  #          "serviceBusConnectionString": "${{vars.INFRA_SERVICE_BUS_NAMESPACE_NAME}}.servicebus.windows.net",
  #          "serviceBusQueueName": "${{vars.INFRA_SERVICE_BUS_QUEUE_NAME}}",
  #          "userAssignedIdentityName": "${{vars.USER_ASSIGNED_IDENTITY_NAME}}"
  #          }'

  #     - name: Deploy Web App to Container App
  #       uses: azure/bicep-deploy@v1
  #       with:
  #         type: deployment
  #         operation: create
  #         name: 'WebAppDeployment-${{ github.run_id }}'
  #         scope: resourceGroup
  #         subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
  #         resource-group-name: ${{ vars.RESOURCE_GROUP_NAME }}
  #         template-file: ./infra/web-app.bicep
  #         parameters: '{
  #          "imageName": "${{ vars.INFRA_ACR_NAME }}.azurecr.io/web-app:${{ github.sha }}",
  #          "managedEnvironmentName": "${{ vars.INFRA_MANAGED_ENVIRONMENT_NAME }}",
  #          "userAssignedIdentityName": "${{vars.USER_ASSIGNED_IDENTITY_NAME}}",
  #          "registryName": "${{vars.INFRA_ACR_NAME}}",
  #          "serviceBusConnectionString": "${{vars.INFRA_SERVICE_BUS_NAMESPACE_NAME}}.servicebus.windows.net",
  #          "serviceBusQueueName": "${{vars.INFRA_SERVICE_BUS_QUEUE_NAME}}"
  #          }'

      # - name: Azure CLI logout
      #   run: az logout